// ─── Nørd Store — Client JS ────────────────
// Placeholder for any shared client-side functionality.
// UXCam custom events can be fired from here.

console.log('Nørd Store loaded.');

// Example: Track custom UXCam events (uncomment when SDK is installed)
// function trackEvent(name, properties) {
//   if (window.UXCam) {
//     UXCam.logEvent(name, properties);
//   }
// }
